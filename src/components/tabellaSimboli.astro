---
export interface Props {
  simboli: string[];
}
import "../scss/globale.scss";
const { simboli } = Astro.props;
import katex, { KatexOptions } from "katex";
const opzioni: KatexOptions = { strict: false };
---

<table class="tabellaSimboli">
  <tr>
    <th>Simbolo</th>
    <th>Come è fatto</th>
  </tr>

  {
    simboli.map((simbolo) => (
      <tr>
        <td set:html={katex.renderToString(simbolo, opzioni)} />
        <td>
          <code>${simbolo}$</code>
        </td>
      </tr>
    ))
  }

  <style>
    table {
      overflow: auto;
      display: block;
    }
  </style>


</table>
