---
layout: "../../../layouts/PostsLayout.astro"
capitolo: 1.1
---

# Probabilità

La **probabilità** è una branca della matematica pratica e non solo teorica; cioè matematica applicata.

La probabilità si occupa di fenomeni casuali e quindi di descrivere degli avvenimenti che sono soggetti al caso. Un esperimento tipico e chiaro è il _lancio del dado_ 🎲.
Il punto dello studio della probabilità è capire che un avvenimento complicato da calcolare (come un lancio di un dado) si può studiare sotto un'altra ottica e anche se non sapremo mai con certezza quale sarà il risultato potremmo capire cosa aspettarci.

## 🎲 Che cos'è un caso

:::def

Il **caso** è la probabilità degli esperimenti che hanno un esito non facilmente prevedibile.

:::

Un sinonimo che useremo è _aleatorio_.

## Richiami teoria degli insiemi

Un insieme è una collezione di oggetti, questi vengono chiamati _elementi dell'insieme_; in genere denotiamo l'insieme con la lettera maiuscola. Es.: $A,B,C$ ecc...

Definizione degli insiemi:

- Insieme per definizione $A = \{ 1,2,3,4,5,6\}$ cioè l'elencazione degli oggetti dell'insieme.
- Con una funziona o con delle proprietà. Es. $A = \{x|x \textup{ ha alcune prop.}\}$

Insiemi numerabili infiniti ($\N$) e non numerabili ($\R$) talmente tanti che non sono numerabili.

Altre cose che potranno tornarci utili:

- **Inclusione** $A\sube B$ ($A$ sottoinsieme proprio di $B$) $A\sub B$ $A$ sottoinsieme proprio di $B$ infatti $\forall  x\in A \textup{ allora} (x\in B)$
- **Complementare** $A^C=\{x|x\not\in A\}$
- **Inters. e Unione** $A\cup B=\{x|x\in A\land x\in B\}\ \ \ A\cap B=\{x|x\in A\lor x\in B\}$
- **Disgiunti** sse. $A\cap B=\empty$
- **Insieme delle parti** $P(A)={B|B\sube A}$
- **sottrazione** degli insiemi
- **partizione** dato `S` siano partizione di s una collezione di insiemi (A) con le seguenti proprietà:
  - A due a due disgiunti
  - Unione di tutti gli insiemi che ricoprono `S`
- Diagrammi di venn.
- Leggi di De Morgan

## Lo Spazio Campionario

:::def
Lo spazio campionario è l'insieme che contiene **tutti i possibili esiti** dell'esperimento probabilistico. In simboli $\Omega$ (omega maiuscola).
:::

È un insieme che contiene tutti i possibili **esiti elementari** dell'esperimento probabilistico. Che sono le cose che rappresentano i possibili esisti.
esempio: se lancio un dato sono l'insieme sarà composto da `1,2,3,4,5,6`.

:::nb
I valori `1,2,3,...` sono come delle etichette che non rappresentano la _quantità_, se il dado fosse stato colorato avremmo potuto usare l'insieme formato dai colori. La cosa importante è ci sia una chiara definizione.
:::

## Modello probabilistico

:::def
È l'oggetto matematico che fornisce la **rappresentazione astratta** dell'esperimento probabilistico.
Il modello è il composto di due oggetti:

1. $\Omega$ - lo spazio campionario.
1. Legge di probabilità $\mathbb P$.

:::

## Legge di probabilità

:::def
La legge di probabilità ($\mathbb P$) è una funzione. L'insieme di partenza sono i sottoinsieme di $\Omega$ e si associano una probabilità. In simboli

$$
\mathbb P(\Omega) \longrightarrow \R
$$

:::

$P(\Omega)$ è la [famiglia delle parti](https://it.wikipedia.org/wiki/Insieme_delle_parti) di omega.

La $\mathbb P$ non è una funzione qualunque ma deve avere queste proprietà:

- $\mathbb P(A) \geq 0$ cioè deve essere **positiva o nulla**
- $\mathbb P(\Omega) = 1$, cioè lo spazio campionario deve essere **finito**. (`1` è indicativo). In soldoni: la somma di tutte le probabilità deve avere il valore che indichiamo (in questo caso `1`)
- Siano $A$ e $B$ due sottoinsiemi di $\Omega$ disgiunti. Se prendo l'unione di $A$ e $B$ deve essere uguale alla somma di probabilità di $a$ e $b$ **additività**. In simboli: $(A\cup B)=P(A)+P(B)$. Questo stesso concetto si deve applicare a sottoinsiemi infiniti. L'idea è quella del concetto di area (probabilità di area).

## Esempi modelli probabilistici

### Esempio 1

_il lancio di una moneta equa_ `->` esperimento probabilistico

---

La domanda: formulazione del modello probabilistico

Ricordiamo per essere un modello devo definire:

- $\Omega$ spazio campionario
- $\mathbb P$ misura di probabilità

$\Omega$ è lo spazio campionario è l'insieme composto da tutti gli esiti possibili, e in più devo scegliere _l'etichetta_ (il nome delle facce). in questo caso sceglieremo $\Omega = \{ \textup{testa}, \textup{ croce} \}$ (per esempio se fossimo inglesi avremmo potuto scrivere _heads, tails_, nulla sarebbe cambiato).

$\mathbb P$ è una funzione che esprime la misura della Probabilità. $\mathbb P(\Omega) = \{ \{t\}, \{c\}, \emptyset, \Omega \}$ specificare $\mathbb P$ vuole dire specificare i seguenti valori:

- $\mathbb P(\emptyset)$
- $\mathbb P(\Omega)$
- $\mathbb P(\{t\})$
- $\mathbb P(\{c\})$

Noi sappiamo già che $\mathbb P(\Omega) = 1$ per la regola 2 cioè la finitezza [vedi](#legge-di-probabilità)

- $\mathbb P(\emptyset) = 0$ lo deduciamo tramite la regola: "se la $\mathbb P(\Omega) = 1$ e tramite la regola dell'attività e della finezza si deduce che la $\mathbb P(\emptyset) = 0$"

- Calcoliamo ora la $\mathbb P$ dei _singoletti_ (`t,c`). Useremo al frase `moneta equa`, e con equità si intende che la moneta non sia truccata e di conseguenza testa e croce hanno la stessa equità, in simboli $\mathbb P(t) = \mathbb P(c)$, noi a intuito sappiamo che il valore sarà uguale a $0.5$. Senza intuizione avremmo potuto pensare: dato che $\mathbb P(\Omega) = 1$ e io sto cercando due numeri uguali che sommano a `1` è subito chiaro che i due saranno uguali a `0.5`.

### Esempio 2

_lancio un dado a 6 facce_

---

- $\Omega$ = \{1,2,3,4,5,6\} (se il dado fosse stato colorato avremmo potuto usare l'iniziale del nome del colore della faccia del dado)
- $\mathbb P(\Omega) = \{\emptyset, \Omega, \{1\}, \{2\}, ..., \{1,2\}, \{1,3\}, ... \}$. Grazie all'utilizzo del [coefficiente binomiale](https://it.wikipedia.org/wiki/Coefficiente_binomiale) riusciamo a contare quanto vale la cardinalità di $\mathbb P$ (cioè $|\mathbb P|$).

Risoluzione:

- $\mathbb P(\emptyset) = 0$
- $\mathbb P(\Omega) = 1$
- Calcolo i _singoletti_ con lo stesso principio dell'esempio 1, l'equità del dado; cioè tutti i _singoletti_ devono essere uguali. $\mathbb P(\textup{singoletto}) = \frac{1}{6}$ (con singoletto intendo `1 o 2 o 3 ecc..`)
- Calcolo la $\mathbb P$ composto da due elementi (per esempio esce il `3 o 4`), dato che $\mathbb P(\{3\} \cup \{4\}) = \mathbb P(\{1\}) + \mathbb P(\{2\}) = \frac{1}{6} + \frac{1}{6}$. Con lo stesso ragionamento prendendo l'insieme `P` composto da $n$ elementi basta fare: $\mathbb P(a) = \frac{|a|}{|n|}$. spesso $n$ e $a$ vengono chiamati $\frac{\textup{casi favorevoli}}{\textup{casi possibili}}$

La $\mathbb P$ si può anche chiamare anche **:d[uniforme discreta]**

### Esempio 3

_lancio 3 volte una moneta equa_

---

- $\Omega = \{ ttt, ccc, tcc, ctc, ...\}$ dove la sua cardinalità è `8` perché $2^3=8$ dove `2` sono i simboli che posso scegliere per ogni posizione e invece `3` è il numero delle posizione
- $\mathbb P(\Omega)$:
  - P dei _singoletti_ $\mathbb P(\{t,t,t\})$ (la moneta è equa) cerco una P la quale per tutti i singoletti siano uguali (come tutti gli atri esempi che abbiamo visto). Lanciare una moneta `3` volte è uguale a lanciare un dado a 8 facce, da ciò si può usare la P uniforme discreta. $\mathbb P(\{ttt\}) = \frac{1}{8}$
  - P "il primo lancio è testa" $\mathbb P = \frac{1}{2}$

### Esempio 4

_lancio 3 volte una moneta equa e ottengo 2 teste_

$B = \{ttc, tct, ctt \}$

:::nb
$ttt$ non è valido perché ha più di 2 teste, se il testo citasse "almeno due teste" avremmo potuto aggiungerlo.
:::

## Probabilità di $\mathbb P$ dedotte dagli assiomi

Se $A,B,C$ sono sottoinsiemi di $\Omega$:

1. $A\sube B → \mathbb P(A)≤\mathbb P(B)$ se A sottoinsieme proprio di **monotonia**, pensando alla $\mathbb P$ come area ci dice che $A$ è più piccolo dell'area di $B$.
2. $\mathbb P(A\cup B)=\mathbb P(A)+\mathbb P(B)-P(A\cap B)$ La legge dall'inclusione esclusione, di non contare due volte la stessa probabilità
3. **Subaddività** viene dalla due se togliamo un numero maggiore di 0 $\mathbb P(A\cup B)≤ \mathbb P(A)+\mathbb P(B)$
4. Sempre il principio di inclusione esclusione

## Esercizi

### Esercizio 1

_Una scatola contiene 3 biglie: una rossa, una verde e una blu. Considero il seguente esperimento: estraggo una biglia, la reimbussolo(parola coniata che significa: "la guardo e la rimetto dentro") e ne estraggo una seconda. Scrivere un possibile modello probabilistico e calcolare la probabilità che le due biglie estratte siano uguali_

Possibile modello

- $\Omega = \{(w_1,w_2)\} \quad w_1 \in \{r,v,b\}$ la cardinalità di $\Omega$ è $3^2 = 9$ (3 possibilità, 2 estrazioni)
- $\mathbb P$ posso usare la P come uniforme discreta? sì perché $\Omega$ è **finito** e le estrazioni casuali continuano a garantire **equità** dei singoletti.

$A =$ l'evento che "le due biglie uguali sono uguali"  
$A = \{(v,v), (b,b), (r,r) = \mathbb P(A) = \frac{3}{9} = \frac{1}{3}\}$

### Esercizio 2

_Un dado a 4 facce equo viene lanciato ripetutamente fino a che esce un numero pari. Scrivere lo spazio campionario per questo esperimento. Quanti sono gli esiti possibili? posso usare la legge uniforme discreta?_

- $\Omega$ dato che non so quanti lanci farò $\Omega$ e quando uscirà un pari. $\Omega = \{\ (w_1, w_2, w_3, ..., w_n) \}$ $w_i \in \{1,2,3,4 \}$ l'unico cosa certa è che l'ultimo numero sarà pari mentre tutti saranno dispari; <br/> $\Omega$ ha cardinalità infinita dato che potrei lanciare infinite volte e ottenere sempre un dispari. Non posso usare la legge uniforme discreta perché $\Omega$ non è finito.
